/**
 * Mermaid scanner — DEPRECATED.
 *
 * For static export (output:"export"), mermaid diagrams are rendered
 * by the standalone scripts in public/js/mermaid-renderer.js + mermaid.min.js.
 *
 * This stub is kept so the barrel export in index.ts doesn't break.
 * Not used in the static export build.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function MermaidScanner() {
  return null;
}
